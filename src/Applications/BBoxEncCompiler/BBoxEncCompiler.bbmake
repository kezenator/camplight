
if (platform() == "Win32") then

    project("BBoxEncCompiler", "Application");

    reference("BBoxUtils");

    include(".");

    sources(find_recursive("*.cpp"));
    headers(find_recursive("*.h"));

end
